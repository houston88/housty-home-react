import React from 'react'

// pure render component
export const Orientation = (props) => (
    <div className='debis-resources-orientation'>
        <div className='container'>
            <h2>Orientation: Test child route</h2>
        </div>
    </div>
);

export default Orientation;
